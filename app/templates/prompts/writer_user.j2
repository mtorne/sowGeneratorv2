Write the body content for section: {{ section_name }}

Use the project data and reference examples below to produce accurate, project-specific prose.
Maintain the structure, tone, and level of detail shown in the reference examples.
Be specific: name the actual components, services, and objectives from the project data.
Do not repeat the section heading. Do not add sub-headings with markdown syntax.

{% if section_name == 'PROJECT PARTICIPANTS' %}
Output EXACTLY ONE sentence — no more, no fewer.
That sentence must introduce the table that follows without listing any names or roles.
Example (do not copy verbatim): "The following table lists the key participants involved in this engagement."
Hard limits: ≤ 20 words, ≤ 1 sentence, NO bullet points, NO line breaks, NO additional paragraphs.
{% endif %}

{% if section_name == 'IN SCOPE APPLICATION' %}
Output EXACTLY ONE sentence (≤ 20 words) stating that the table below details the in-scope application.
Do NOT describe the application. Do NOT mention technical details — the table captures them.
Example: "The table below details the application in scope for this engagement."
Hard limits: ≤ 1 sentence, no bullet points, no line breaks, no additional paragraphs.
{% endif %}

{% if section_name == 'PROJECT OVERVIEW' %}
Write 1-2 introductory sentences (maximum 50 words total) summarising the engagement at a high level:
who the customer is, what the application does, and the migration objective.
Do NOT write multiple paragraphs. Do NOT list deliverables.
Do NOT use these words: jointly, joint, Oracle Labs, Oracle team.
{% endif %}

{% if section_name == 'FUTURE STATE ARCHITECTURE' %}
Output EXACTLY ONE sentence (≤ 30 words) introducing the target architecture at a high level.
This heading is a container section — sub-sections below describe the details.
Do NOT describe components, services, or topology — those belong in the sub-sections.
Do NOT write multiple paragraphs or bullet points.
Example: "The target architecture leverages Oracle Cloud Infrastructure for a highly available, multi-region deployment."
Hard limits: ≤ 1 sentence, no bullet points, no line breaks, no additional paragraphs.
{% endif %}

{% if section_name == 'SCOPE' %}
Write exactly THREE labeled blocks — one for each scope perspective box below.
Each block: label on its own line (no colon), then 2-4 sentences of specific scope content.
Use these EXACT labels (copy verbatim, one per line):

Initial Understanding
Customer Desired Outcome
Agreed Outcome

Guidelines:
- Initial Understanding: what is currently known about what the customer needs; what must be
  validated together before work starts.
- Customer Desired Outcome: the end-state the customer wants to achieve from their perspective
  (business value, performance, operational goals).
- Agreed Outcome: the mutually agreed deliverables and success criteria for this engagement.

Strict rules:
- Do NOT use the words: joint, jointly, Oracle Labs, Oracle team.
- Do NOT write any prose before the first label.
- Do NOT add markdown syntax (no #, *, –).
- Separate the three blocks with a blank line.
- Maximum 50 words per block body.
- Do NOT start any block body with the customer name — the scope box label
  already ends with the customer name, and repeating it creates visual
  duplication (e.g. "…of Acme: Acme aims to…"). Begin with a verb or
  descriptive noun phrase instead (e.g. "The current platform…", "Migration
  of…", "A fully managed…").
{% endif %}

{% if section_name == 'ARCHITECTURE COMPONENTS' %}
Group content using EXACTLY these sub-topic labels (plain-text, each on its own line):
Networking | Security | Compute | Storage and Databases | DevOps and Management

After each label, add a "Key Highlights" sub-topic summarising the most important
architectural decisions (e.g. multi-cloud strategy, hub-and-spoke, HA pattern).

{% if diagram_components %}
IMPORTANT: The target architecture diagram was analysed automatically. Use ONLY the real
services and components listed below — do not invent anything not present here.

Diagram-identified components (JSON):
{{ diagram_components | tojson(indent=2) }}
{% else %}
Reference only components explicitly mentioned in the project data.
{% endif %}

WRITING STYLE — follow this pattern for each component (definition style):
Write each component as: "**Component Name:** 1-2 sentence description of its role."
Example:
  Virtual Cloud Networks (VCNs): Virtual networks in OCI providing network isolation,
  subnet segmentation, and security-list-controlled traffic between tiers.
  Local Peering Gateways (LPGs): Inter-VCN connectivity within the same region for
  east-west traffic between hub and spoke networks.

Rules:
- List 2-5 components per sub-topic with definition-style descriptions.
- Write in professional prose — do NOT use dash-prefixed bullet lists.
- Each component description must be project-specific, not generic boilerplate.
- Do NOT say "No specific components identified" — if a sub-topic has no components
  from the project data, write "PENDING TO REVIEW" as the description.
{% endif %}

{% if section_name == 'ARCHITECTURE DEPLOYMENT OVERVIEW' %}
Write EXACTLY ONE short paragraph (3-5 sentences, maximum 80 words) summarising the
deployment topology: which cloud regions are used, the networking model (e.g. hub-and-spoke),
the key services, and the HA/DR approach.
Be specific — name real services from the project data. Do NOT exceed one paragraph.
Do NOT use bullet points, numbered lists, or markdown headings.
Do NOT repeat content that belongs in Architecture Components or Implementation Details.
{% endif %}

{% if section_name == 'IMPLEMENTATION DETAILS' %}
Group content into the following sub-topics, each introduced with a plain-text label on its own line:
Networking | Security | Compute | Storage and Databases | DevOps and Management

For each sub-topic, list the specific target configuration settings that will be applied:
exact versions, CIDR ranges, instance shapes/OCPUs/memory, service options, and key parameters.
Draw values from the project data JSON wherever possible.

For any configuration value that is NOT specified or cannot be inferred from the project data,
write the exact literal string: PENDING TO REVIEW
(this marker will be highlighted in red in the document for easy identification).

Provide 3-5 settings per sub-topic. Be concrete — avoid generic filler sentences.
{% endif %}

{% if section_name == 'CURRENTLY USED TECHNOLOGY STACK' %}
Write EXACTLY ONE introductory sentence (≤ 30 words) stating that the table below lists the customer's
current technology stack. Do not enumerate technologies in the text — the table already does this.
Hard limits: ≤ 1 sentence, no bullet points, no line breaks, no additional paragraphs.
Example: "The table below outlines the technologies currently in use for this application."
{% endif %}

{% if section_name == 'OCI SERVICE SIZING AND AMOUNTS' %}
Write EXACTLY ONE introductory sentence (≤ 35 words) referring the reader to the table below,
which captures the proposed OCI service sizes and quantities for this engagement.
Do NOT enumerate services in the text — the table already does this.
Hard limits: ≤ 1 sentence, no bullet points, no line breaks, no additional paragraphs.
Example: "The table below details the proposed OCI services, sizing, and quantities for this migration."
{% endif %}

{% if section_name == 'STATUS AND NEXT STEPS' %}
Write a concise project status summary.

Start with ONE plain introductory sentence (≤ 20 words) describing overall status.

Then write EXACTLY two groups of bullet points:

First group header (plain text, on its own line): Completed
List what has already been achieved. Use "– " as the bullet prefix.
Maximum 4 bullets. Each bullet MUST be ≤ 10 words — short, punchy facts.
Example style: "– Agreed on target architecture based on MAA Platinum"

Second group header (plain text, on its own line): Next Steps
List concrete actions still required. Use "– " as the bullet prefix.
Maximum 4 bullets. Each bullet MUST be ≤ 10 words.
Example style: "– Confirm BOM and service sizing"

TONE: Write like a manual status update — factual, telegram-style, no full sentences.
Do NOT over-qualify or add unnecessary context.
Hard rules: no prose paragraphs (except the intro), no markdown headings (#).
Draw facts from the project data JSON.
{% endif %}

{% if section_name == 'CURRENT STATE ARCHITECTURE DESCRIPTION' %}
Write a concise, flowing PROSE description of the CURRENT (pre-migration) architecture.

CRITICAL GROUNDING RULE:
{% if diagram_components %}
The current-state architecture diagram was analysed automatically.
Describe ONLY the components listed below — do NOT include any OCI services, target-state
components, or anything not present in the current diagram.  The context JSON also contains
architecture_analysis.target (the future OCI design); ignore it entirely for this section.

Diagram-identified current components (JSON):
{{ diagram_components | tojson(indent=2) }}
{% else %}
Draw facts exclusively from architecture_analysis.current in the project data JSON.
Do NOT reference architecture_analysis.target or any OCI services — this section
describes the existing environment before migration, not the future OCI design.
{% endif %}

WRITING STYLE:
Write 2-4 paragraphs of professional narrative prose — as if a human architect is describing
the current environment to a new team member.  Cover these areas in natural flowing text:
  1. What platform the application runs on today (cloud provider, compute, containers)
  2. Database tier (product, edition, HA setup)
  3. Networking and load balancing
  4. Current HA/DR and backup approach (if known)

Example tone (do NOT copy — adapt to actual project):
"The current IFS Cloud architecture runs on Microsoft Azure, where Oracle Database is used as
the RDBMS. Each customer has up to 4 environments (Dev, Test, Staging, Production). The
application tier is containerised on Azure Kubernetes Service (AKS) with horizontal pod
autoscaling enabled."

STRICT RULES:
- Do NOT use bullet points, dashes, or numbered lists.
- Do NOT use markdown headings (#, ##).
- Write in third person, present tense, professional tone.
- Maximum 120 words total.
{% endif %}

{% if section_name == 'MANAGED SERVICES CONFIGURATION' %}
Write a concise description of the OCI managed services that Oracle will configure and operate
as part of this engagement.

You MUST explicitly name the specific OCI managed services from the list below that are
relevant to this project (based on the project data JSON).  Do NOT use generic terms like
"managed database" or "container platform" — always use the official OCI service name.

Key OCI managed services to consider (only include those relevant to this project):
- Oracle Autonomous Database (ADB / ATP / ADW)
- MySQL Database Service (MDS / MySQL HeatWave)
- Oracle Database Cloud Service (DBCS / dbsystem / ExaCS)
- Oracle Kubernetes Engine (OKE)
- Oracle Container Instances (OCI)
- Oracle Cache (Redis-compatible managed cache)
- Oracle Functions (serverless)
- OCI Streaming (managed Kafka-compatible)
- Oracle Integration Cloud (OIC)
- OCI API Gateway
- Oracle GoldenGate
- OCI Vault (key management)

Format: Write 2-4 sentences. After the introductory sentence, add one bullet per selected
managed service using "– " prefix, naming the service and its role in the architecture.
Do NOT add markdown headings. Do NOT mention services not relevant to the project.
{% endif %}

{% if section_name == 'ARCHITECT REVIEW' %}
Write a structured architect review for this auto-generated Statement of Work.
Use EXACTLY these four sub-topic labels (plain-text, each on its own line):
Generation Quality | Data Gaps | Next Steps | Recommendations

Generation Quality
Assess what was generated well (e.g. architecture components, implementation details) and
identify any sections that appear incomplete or based on insufficient project data.
Limit to 2-3 concise bullet sentences.

Data Gaps
List specific project data fields that are missing, ambiguous, or could not be inferred:
e.g. CIDR ranges, database version, instance sizing, application tier details.
Each gap as a short bullet (use "–" prefix). Maximum 6 bullets.

Next Steps
List concrete actions the architect must take to complete this document before review:
e.g. "Fill CIDR ranges in Implementation Details", "Confirm DB tier sizing with customer".
Number each step (1., 2., ...). Maximum 5 steps.

Recommendations
Provide 2-3 technical or process recommendations for this engagement:
e.g. architecture trade-offs, risk mitigation, governance suggestions.
Write in professional, concise language. This section is internal — not customer-facing.
{% endif %}

{% if section_name == 'MILESTONE PLAN' %}
Generate project milestones to be appended to the existing milestone table.
The template already contains standard milestones — YOUR milestones will be ADDED
as additional rows. Do NOT repeat standard milestones (kick-off, arch diagram,
go-live) that the template already includes.

Derive milestones from the gap between current and target architecture
(use architecture_analysis in the project data JSON if present).

Milestone examples (adapt to the actual project):
– OCI tenancy and networking provisioned
– Database migration to Exadata/Base DB completed
– Application tier deployed on OKE/Compute
– Performance and security testing completed
– DR and backup configuration validated
– Production cutover rehearsal completed

Output a JSON object matching this EXACT schema (example — use 4-8 milestones):
{
  "milestones": [
    {
      "milestone": "OCI tenancy and networking provisioned",
      "target_date": "PENDING TO REVIEW",
      "completed": "",
      "comments": ""
    },
    {
      "milestone": "Database migration to Exadata completed",
      "target_date": "PENDING TO REVIEW",
      "completed": "",
      "comments": "Includes Data Guard standby"
    }
  ]
}

JSON rules:
- "milestone" is a short event/deliverable name (≤ 10 words).
- "target_date" is DD-MM-YYYY or "PENDING TO REVIEW" if unknown.
- "completed" is DD-MM-YYYY or empty string if not yet completed.
- "comments" is a brief optional note — empty string if nothing to add.
- Total milestones: 4 to 8.  No extra keys.
{% endif %}

{% if section_name == 'HIGH AVAILABILITY' %}
Write a concise High Availability design for this deployment.
Base your response on architecture_analysis.target in the project data JSON (if present).

{% if diagram_components %}
CRITICAL GROUNDING RULE — STRICTLY ENFORCED:
The target architecture diagram was analysed automatically.
You MUST use ONLY the services, components, and cloud providers identified in the diagram below.
Do NOT invent, add, or infer any service not explicitly present in this list.
If a service you would normally mention (e.g. an OCI or Azure load balancer) is NOT in the diagram,
do NOT reference it. Stick exclusively to what is present.

Diagram-identified target components (JSON):
{{ diagram_components | tojson(indent=2) }}
{% else %}
Base your response on architecture_analysis.target in the project data JSON.
Name only services and components that are explicitly mentioned in the project data.
{% endif %}

Sub-topic content guidelines:
- oci_ha_capabilities: Name the specific HA constructs leveraged (e.g. Fault Domains, Availability
  Domains, Active Data Guard, load balancers) — only from the diagram/project data.
- redundancy_architecture: Describe how the target architecture achieves redundancy at each tier:
  compute, database, storage, and network. Reference actual diagram components only.
- failover_strategy: Explain the failover mechanism (automatic vs. manual, DNS-based, Full Stack DR).
  Include health checks, standby configuration, or replication details as applicable.
- rto_rpo_targets: State the Recovery Time Objective and Recovery Point Objective.
  If not specified in the project data, each entry must be exactly: "PENDING TO REVIEW"

Output a JSON object matching this EXACT schema:
{
  "oci_ha_capabilities": ["<bullet>", "..."],
  "redundancy_architecture": ["<bullet>", "..."],
  "failover_strategy": ["<bullet>", "..."],
  "rto_rpo_targets": ["<bullet>", "..."]
}

JSON rules:
- Each field must be a list of 2-4 concise bullet strings (no bullet-prefix characters).
- Write project-specific content only — no generic boilerplate or invented services.
- No extra keys.
{% endif %}

{% if section_name == 'BACKUP STRATEGY' %}
Write a concise Backup Strategy for this deployment.
Base your response on architecture_analysis in the project data JSON (both current and target if present).

{% if diagram_components %}
CRITICAL GROUNDING RULE — STRICTLY ENFORCED:
Use ONLY the data-tier and application components identified in the diagram below.
Do NOT reference services not present in this list.

Diagram-identified target components (JSON):
{{ diagram_components | tojson(indent=2) }}
{% else %}
Name only components explicitly mentioned in the project data.
{% endif %}

Sub-topic content guidelines:
- data_backup: Identify all data-tier components requiring backup (databases, file shares, volumes)
  that are present in the diagram/project data. Name the backup service for each. Be project-specific.
- application_backup: Describe how application state is backed up (container images, persistent
  volumes, secrets, config files). Reference actual diagram components only.
- recovery_procedures: Summarise the restore procedure as numbered steps, e.g.
  "1. Initiate restore from latest snapshot", "2. Verify DB integrity …".
- retention_policy: State backup frequency and retention period per tier.
  If not specified in the project data, each entry must be exactly: "PENDING TO REVIEW"

Output a JSON object matching this EXACT schema:
{
  "data_backup": ["<bullet>", "..."],
  "application_backup": ["<bullet>", "..."],
  "recovery_procedures": ["<numbered step>", "..."],
  "retention_policy": ["<bullet>", "..."]
}

JSON rules:
- Each field must be a list of 2-4 concise strings (no bullet-prefix characters).
- Write project-specific content only — no invented services.
- No extra keys.
{% endif %}

{% if section_name == 'DISASTER RECOVERY' %}
Write a concise Disaster Recovery section for this deployment.
Base your response on architecture_analysis and project data JSON.

{% if diagram_components %}
CRITICAL GROUNDING RULE — STRICTLY ENFORCED:
Use ONLY the components, services, and cloud providers identified in the diagram below.
Do NOT reference regions, replication mechanisms, or DR services not present in this list.

Diagram-identified target components (JSON):
{{ diagram_components | tojson(indent=2) }}
{% else %}
Name only services and regions explicitly mentioned in the project data.
{% endif %}

Sub-topic content guidelines:
- dr_strategy: State the DR tier (active-passive, active-active, warm standby) and the specific
  mechanism used (e.g. Data Guard, Full Stack DR, cross-region replication). Use only what is
  present in the diagram/project data.
- geographic_redundancy: Name the specific regions and availability domains involved (from the
  diagram). Describe how workloads are distributed across the DR topology.
- data_replication: Explain how data is replicated to the DR site using only mechanisms present
  in the diagram (Data Guard, InnoDB Cluster, cross-region replication, etc.).
- dr_testing_plan: State the frequency of DR drills, recovery procedure documentation location,
  and RTO/RPO targets.
  If not specified in the project data, each entry must be exactly: "PENDING TO REVIEW"

Output a JSON object matching this EXACT schema:
{
  "dr_strategy": ["<bullet>", "..."],
  "geographic_redundancy": ["<bullet>", "..."],
  "data_replication": ["<bullet>", "..."],
  "dr_testing_plan": ["<bullet>", "..."]
}

JSON rules:
- Each field must be a list of 2-4 concise strings (no bullet-prefix characters).
- Write project-specific content only — no invented services or regions.
- No extra keys.
{% endif %}

Project data JSON:
{{ context_json }}

Retrieved similar section examples (content reference only — follow the output format rules above, not the example format):
{{ examples if examples else 'No retrieved examples available.' }}

{% if json_output %}
Output ONLY the raw JSON object as specified above. No prose text before or after. No markdown code fences.
{% else %}
Output only the section body content. Do not add a title or heading line.
{% endif %}
